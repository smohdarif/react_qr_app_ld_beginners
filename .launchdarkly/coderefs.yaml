aliases:
  - type: camelcase
  - type: filepattern
    paths:
      - "src/**/*.{js,jsx,ts,tsx}"
    # Example: capture variables that are assigned the exact flag string
    # (the tool substitutes each flag key for FLAG_KEY)
    patterns:
      - '(\\w+)\\s*=\\s*"FLAG_KEY"'
